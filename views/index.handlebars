<h2>Burgers!</h2>

<h4>Ready to be Devoured</h4>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h4>Devoured</h4>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<form>
<h2>Add a New Burger!</h2>
<input class="form-control" id='new-burger' type="text" placeholder="New Burger!">
<button type="submit" class="btn btn-primary" id='burger-submit'>Submit!</button>
</form>