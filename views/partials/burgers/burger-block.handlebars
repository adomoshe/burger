<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-devour="{{devour}}">
		{{#if devour}}Devour!{{else}}Reorder!{{/if}}
	</button>

	<button class="delete" data-id="{{this.id}}">DELETE!</button>
</li>
